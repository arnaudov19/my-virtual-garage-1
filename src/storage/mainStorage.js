/*
const mainStorage = {
  currentPage: 1,
};

export const getCurrentPage = () => {
  return mainStorage.currentPage;
};

export const setCurrentPage = (pageNumber) => {
  console.log("setting the current page", pageNumber);
  mainStorage.currentPage = pageNumber;
};
*/
