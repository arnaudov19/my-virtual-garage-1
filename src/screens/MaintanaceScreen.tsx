import React, { useState } from "react"
import { ChangeScreenComponent } from "./ChangeScreenComponent"
type Props = {
    onSetPage: (page: number) => void
}

const MaintanaceScreen = (props: Props) => {
    return (
        <div>
            <h1>Hey</h1>
        </div>
    )
}

export default MaintanaceScreen
